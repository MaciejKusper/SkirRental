package skirental.database.dao;

import com.j256.ormlite.support.ConnectionSource;

public class OrdersDao extends CommonDao {
    public OrdersDao(ConnectionSource connectionSource) {
        super(connectionSource);
    }
}
